import { useState } from "react";
import { HeaderWrapper, LoginButton, RegButton } from "./HeaderStyle";

export default function Header(){
    const [isLoggedIn, setIsLoggedIn] = useState(false);
    return(
        <>
            <HeaderWrapper>
                {
                    
                }
            </HeaderWrapper>
        </>
    )
}